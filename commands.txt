

minikube start
minikube status

git add . 
git commit -m "adding empty files"
git checkout -b dev
git checkout -b Dokerfile

python -m venv .venv
 .\.venv\Scripts\Activate  
pip install -r .\requirements.txt

docker build -t michaelfriedman1/streamlit_test:v2 .

docker images

docker run -d  -p 8080:8501 michaelfriedman1/streamlit_test:v2

docker push michaelfriedman1/streamlit_test:v2

git add .

git commit -m "completed docker file"

git checkout main 

git merge Dokerfile

git checkout -b pod_yaml

git add .

git commit -m "the pod.yaml completed"

git checkout main 

git merge pod_yaml

git checkout -b change

git add . 

git commit -m "changeing the docker file"

git checkout main 

git merge change

git checkout -b service_yaml

git add .

git commit -m "the service.yaml completed"

git checkout main 

git merge service_yaml
